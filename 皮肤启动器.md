# 鲸鱼娘皮肤启动器（maid-atelier · 深海女仆工坊）

DeepSeek Harness Web GUI 的**鲸鱼娘皮肤一键启动器**。本目录同时分发：

- **启动器脚本** `启动鲸鱼娘皮肤.bat`：一键把皮肤安装进 `web` profile 并启动 `dsh web`
- **皮肤本体** `maid-atelier/`：来自 [Small-tailqwq/dsh-deep-whale](https://github.com/Small-tailqwq/dsh-deep-whale)，含预构建 `lib/`，激活不依赖远程资源
- **辅助脚本** `scripts/ensure-skin.ps1`、`scripts/make-desktop-shortcut.ps1`
- **桌面图标** `maid-icon.ico`：女仆鲸鱼娘大头像

## 效果

| 亮色模式 | 暗色模式 |
|---|---|
| ![light](maid-atelier/preview/light.webp) | ![dark](maid-atelier/preview/dark.webp) |

双女仆工坊场景对话背景（亮/暗自动切换）、深海蓝蕾丝 UI 覆盖层、Q 版侧栏角色。

## 前置

- DeepSeek Harness checkout（自动探测 `D:\DEEPSEEK harness\deepseek-harness`、`D:\AI\deepseek\deepseek-harness`，也可用环境变量 `DSH_HARNESS` 指定）
- Node（自动探测 `D:\DEEPSEEK harness\node`、`D:\chajian`，也可用 `DSH_NODE_DIR` 指定，否则走 PATH）
- `pnpm` 在 PATH 上
- `DSH_HOME` 缺省为 `%USERPROFILE%\.dsh`，可用环境变量覆盖

## 使用

1. **双击 `启动鲸鱼娘皮肤.bat`**：首次运行会自动把 `maid-atelier` 装进 `web` profile（`dsh plugin --profile web add`，幂等），之后每次启动自检；然后启动 `dsh web`，浏览器打开 <http://127.0.0.1:3080>。**关闭窗口即停止服务。**

2. **（可选）桌面快捷方式**（带鲸鱼娘大头图标）：

   ```powershell
   powershell -ExecutionPolicy Bypass -File scripts\make-desktop-shortcut.ps1
   ```

   生成的快捷方式指向本启动器。也可以在 ensure-skin 时顺带创建：

   ```powershell
   powershell -ExecutionPolicy Bypass -File scripts\ensure-skin.ps1 -SkinDir .\maid-atelier -CreateShortcut
   ```

3. **卸载皮肤**（在 harness 目录）：

   ```sh
   pnpm dsh plugin --profile web remove @dsh-external/dsh-client-ui-skin-maid-atelier
   ```

   重启 `dsh web` 即完全复原。

## 皮肤更新

重新拉取上游皮肤包并替换 `maid-atelier/`：

```sh
git clone https://github.com/Small-tailqwq/dsh-deep-whale
# 用新内容替换本仓库 maid-atelier/ 后，下次启动启动器会自动重新 link
```

## 许可与署名

皮肤为**衍生创作**，整体以 **CC BY-NC-SA 4.0**（署名-非商业性使用-相同方式共享）发布，**禁止商业性使用**。署名链（详见 `maid-atelier/NOTICE` 与 `maid-atelier/LICENSE`）：

1. **上善** — 鲸鱼娘角色形象原作（[Pixiv](https://www.pixiv.net/users/62155430) · [Bilibili：上善无形](https://b23.tv/8h5L4xz)）
2. **ZipZipPipe** — 加入 DeepSeek 元素的二创设计（[Pixiv](https://www.pixiv.net/users/18604994) · [Bilibili](https://b23.tv/Pnw6nG8)）
3. **Small-tailqwq** — 皮肤工程与 DeepSeek 元素再设计（[dsh-deep-whale](https://github.com/Small-tailqwq/dsh-deep-whale)）

启动器脚本（`启动鲸鱼娘皮肤.bat`、`scripts/` 下两个 ps1）与 `maid-icon.ico` 为本仓库自产内容，同样以 CC BY-NC-SA 4.0 发布。
